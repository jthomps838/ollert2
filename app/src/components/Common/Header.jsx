const Header = () => {
    return (
        <header className='main_header'>
            <aside>Logo</aside>
            <nav>
                <button>Login</button>
            </nav>
        </header>
    );
};

export default Header;
